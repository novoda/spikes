<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

  <LinearLayout
    style="@style/Chat.Message.Metadata"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
      android:id="@+id/messageAuthorName"
      style="@style/Chat.Message.Author"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content" />

    <TextView
      android:id="@+id/messageTime"
      style="@style/Chat.Message.Time"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content" />

  </LinearLayout>

  <RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/chat_message_body_padding">

    <ImageView
      android:id="@+id/messageAuthorImage"
      android:layout_width="@dimen/chat_message_author_image"
      android:layout_height="@dimen/chat_message_author_image"
      android:layout_alignParentStart="true"
      android:scaleType="fitCenter" />

    <TextView
      android:id="@+id/messageBody"
      style="@style/Chat.Message.Body"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_alignTop="@id/messageAuthorImage"
      android:layout_marginStart="@dimen/chat_message_body_margin"
      android:layout_toEndOf="@id/messageAuthorImage" />

  </RelativeLayout>

</merge>

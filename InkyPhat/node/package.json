{
  "name": "inkyphat",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "node": " node build/Main.js",
    "build_run": "npm run build && npm run node"
  },
  "dependencies": {
    "rpi-gpio": "^0.8.1",
    "spi-device": "^0.2.6"
  }
}

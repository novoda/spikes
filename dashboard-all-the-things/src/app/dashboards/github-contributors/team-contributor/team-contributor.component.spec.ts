import { TeamContributorComponent } from './team-contributor.component';

describe('Component: TeamContributor', () => {
  it('creates an instance', () => {
    let component = new TeamContributorComponent();
    expect(component).toBeTruthy();
  });
});

/* tslint:disable:no-unused-variable */

import { SystemClock } from './system-clock';

describe('SystemClock', () => {
  it('should create an instance', () => {
    expect(new SystemClock()).toBeTruthy();
  });
});

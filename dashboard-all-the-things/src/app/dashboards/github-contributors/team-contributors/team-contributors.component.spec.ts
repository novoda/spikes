/* tslint:disable:no-unused-variable */

import { TeamContributorsComponent } from './team-contributors.component';

describe('Component: TeamContributors', () => {
  it('creates an instance', () => {
    let component = new TeamContributorsComponent(null);
    expect(component).toBeTruthy();
  });
});


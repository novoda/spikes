buildscript {
    apply from: '../dependencies.gradle'

    repositories {
        jcenter()
    }
    dependencies {
        classpath gradlePlugins.gradle
    }
}

apply from: '../dependencies.gradle'

allprojects {
    repositories {
        jcenter()
    }
}

apply plugin: 'com.android.application'

android {
  compileSdkVersion 25
  buildToolsVersion '25.0.2'

  defaultConfig {
    applicationId 'com.novoda.monkeytrap'
    minSdkVersion 16
    targetSdkVersion 22 // Keep as is so that we don't have permission problems
    versionCode 1
    versionName '1.0'
  }
}

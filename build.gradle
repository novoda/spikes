ext {
  isLocalDeploy = project.hasProperty('local')
  pluginGroup = 'com.novoda.build-properties'
  pluginVersion = isLocalDeploy ? 'LOCAL' : '0.9.0'
}

subprojects {
  buildscript {
    repositories {
      jcenter()
    }
  }
  repositories {
    jcenter()
  }
}



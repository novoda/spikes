subprojects {
  buildscript {
    repositories {
      jcenter()
    }
  }
  repositories {
    jcenter()
  }
}

ext {
  pluginGroup = 'com.novoda.build-properties-plugin'
  pluginVersion = '0.9.0'
}

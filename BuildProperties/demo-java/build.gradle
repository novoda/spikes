apply plugin: 'java'
apply plugin: 'com.novoda.buildproperties'

buildProperties {
    file = 'props/simple.properties'
}

// the expected behavior of the plugin would be to add the read properties to project.buildProperties
println project.buildProperties.getInt('someNumber')

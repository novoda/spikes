<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.novoda.todoapp">

  <application
    android:name=".TodoApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <activity
      android:name=".tasks.TasksActivity"
      android:label="@string/app_name">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity android:name=".task.TaskDetailActivity" />
    <activity
      android:name=".task.edit.EditTaskActivity"
      android:windowSoftInputMode="adjustResize" />
    <activity
      android:name=".task.newtask.NewTaskActivity"
      android:windowSoftInputMode="adjustResize" />
    <activity
      android:name=".statistics.StatisticsActivity" />

  </application>

</manifest>
